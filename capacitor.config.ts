import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ottawarehab.app',
  appName: 'Ottawa Rehab',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
